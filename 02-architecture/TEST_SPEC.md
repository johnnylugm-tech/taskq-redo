# TEST_SPEC.md — Named Test-Case Catalog (Single Source of Truth)

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1 `TEST_INVENTORY.yaml`
> are preserved as-is where specified. This is the SINGLE SOURCE OF TRUTH for
> all test traceability checks at Gates 1-4 (v2.6 unified D4). Validated by
> Agent B (TECH_LEAD) before P2 exit. P3 Agent A implements tests FROM this
> catalog — not ad-hoc.

**Project**: taskq-api
**SRS version**: 1.0.0 (2026-07-30, transcribed verbatim from `SPEC.md`)
**SAD version**: 1.0 (2026-09-01)
**TEST_INVENTORY.yaml version**: 1.1 (P1 Naming Authority, 92 entries)
**Generated**: 2026-09-01
**Active NFR Patterns**: NP-01, NP-02, NP-03, NP-04, NP-06, NP-07, NP-08, NP-10, NP-12, NP-13, NP-14, NP-15

---

## NFR Pattern Activation (Step 1 + Step 1b + Step 1c Output)

> The table enumerates all 15 patterns (NP-01..NP-15). Pattern triggers are
> sourced from: `SRS.md`/`SPEC.md` keyword scan (Step 1); SAD §2 module /
> §5 SAB block architecture-risk triggers (Step 1b — shared mutable state
> forces NP-13; subprocess executor forces NP-15; DB dependency forces
> NP-07); SAD §6 STRIDE threat rows (Step 1c — every threat's own
> `verified_by` test fires NP-08 regardless of SRS keywords). Steps 1b/1c
> cases are mandatory and live under `tests/integration/` or `tests/unit/`
> per their declared layer.

| Pattern | Trigger (SRS keyword / SAD module / SEC threat) | Activated |
|---------|--------------------------------------------------|-----------|
| NP-01 (auth 401)          | SRS: "401" + FR-03 AC-3.1/AC-3.2                 | ✅ |
| NP-02 (authz 403)         | SRS: "403" + FR-04 AC-4.1/AC-4.2                 | ✅ |
| NP-03 (rate limit 429)    | SRS: "429" + FR-05 AC-5.1                        | ✅ |
| NP-04 (validation 422)    | SRS: "input validation" + FR-01 AC-1.2           | ✅ |
| NP-05 (idempotency)       | not found in scope (single-write, not idempotent-keyed surface) | ❌ |
| NP-06 (latency SLA)       | SRS: "p95" + NFR-01 AC-N1.1/AC-N1.2              | ✅ |
| NP-07 (dependency fault)  | SAD: `taskq_api.repository.session` (DB engine) + NFR-04 AC-N4.2 | ✅ |
| NP-08 (security attack)   | SEC: T-01..T-10 (STRIDE — spoofing, tampering, EoP, info-disclosure, DoS, repudiation) | ✅ |
| NP-09 (audit log)         | not in scope (correlation_id covers request tracing, not full audit trail) | ❌ |
| NP-10 (data round-trip)   | SRS: "byte-identical" + FR-07 AC-7.2 (v3 data migration) | ✅ |
| NP-11 (backward compat)   | not applicable (no v1 service to maintain)      | ❌ |
| NP-12 (pagination)        | SRS: "cursor-based" + FR-01 AC-1.6              | ✅ |
| NP-13 (concurrency)       | SAD: `taskq_api.repository.rate_repo` (row-level lock) + FR-05 AC-5.4; SAD: `taskq_api.service.runner` (TaskGroup) + FR-08 AC-8.2 | ✅ |
| NP-14 (encryption)        | SRS: "SHA-256" + FR-03 AC-3.3 (hashed storage)   | ✅ |
| NP-15 (timeout)           | SAD: `taskq_api.service.runner` (subprocess kill) + FR-02 AC-2.5 / FR-08 AC-8.3 | ✅ |

---

## Functional Requirement Test Cases

### FR-01: Task resource CRUD API — POST/GET/LIST/DELETE `/v1/tasks` with cursor pagination (limit 50, cap 200); 422 validation / 404 unknown id / 409 duplicate name.

**Classification**: API_ENDPOINT
**Active Patterns**: NP-04 (422 validation), NP-12 (cursor pagination)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_post_task_returns_201_with_id` | command="echo hello"; name="happy-create-1"; api_key="write-key"; scope="write" | happy_path | Q1 [AC-1.1] |
| 2 | `test_post_task_validation_violations_returns_422` | command=""; name="bad-empty"; api_key="write-key"; scope="write"; violation="empty_command" | validation | Q2 [AC-1.2] |
| 3 | `test_get_task_unknown_id_returns_404` | task_id="00000000-0000-0000-0000-000000000000"; api_key="read-key"; scope="read" | failure | Q2 [AC-1.3] |
| 4 | `test_post_task_duplicate_name_returns_409` | command="echo dup"; name="dup-name-1"; api_key="write-key"; scope="write"; first_exists="true" | validation | Q2 [AC-1.4] |
| 5 | `test_list_task_limit_above_200_returns_422` | limit="300"; api_key="read-key"; scope="read" | boundary | Q3 [AC-1.5] |
| 6 | `test_list_pagination_is_cursor_based` | cursor="some-uuid-value"; limit="50"; api_key="read-key"; scope="read"; rejects_offset_param="true" | nfr_pattern | Q6 (NP-12) [AC-1.6] |
| 7 | `test_delete_task_removes_results_in_same_transaction` | task_id="uuid-with-results-1"; api_key="admin-key"; scope="admin"; has_results_row="true" | integration | Q7 [AC-1.7] |

**Sub-assertions** — predicate over a case's declared Inputs / the production `result`. Predicates are ALWAYS Python-expression syntax (e.g. `len(result) == 4`), regardless of implementation language — they are spec-level declarations evaluated by the P2 self-consistency engine, not test code. Every predicate below is TRUE for the case inputs it `applies_to` — the spec asserts a property of the declared input value, not the implementation's response.

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR01-empty-cmd-violates | `len(command) == 0` | 2 |
| FR01-limit-cap-200-over | `limit == "300"` | 5 |
| FR01-cursor-not-offset | `cursor == "some-uuid-value"` | 6 |
| FR01-name-unique | `name == "dup-name-1"` | 4 |
| FR01-admin-scope-deletes | `scope == "admin"` | 7 |
| FR01-write-scope-creates | `scope == "write"` | 1 |
| FR01-happy-command-nonempty | `len(command) > 0` | 1 |

**Properties** — OPTIONAL (Direction B). FR-01 has no clean algebraic invariant (CRUD endpoints are not round-trip; cursor pagination is total order but not algebraic). Skip.

---

### FR-02: Task execution endpoint — `POST /v1/tasks/{id}/run` → 202 + `run_id`; `asyncio.create_subprocess_exec`; `shell=True` forbidden; writes `task_results` rows; `GET /v1/tasks/{id}/runs` returns history.

**Classification**: STATE_MACHINE
**Active Patterns**: NP-15 (timeout), NP-08 (T-05 shell metachar)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_post_run_returns_202_with_run_id` | task_id="uuid-ready-1"; command="echo hi"; api_key="write-key"; scope="write" | happy_path | Q1 [AC-2.1] |
| 2 | `test_shell_true_absent_from_src_tree` | src_root="03-development/src"; forbidden_token="shell=True" | security_control | Q6 (NP-08) [AC-2.2] |
| 3 | `test_state_machine_transitions` | command="echo done"; initial_status="pending"; expected_terminal="done" | state_transition | Q4 [AC-2.3] |
| 4 | `test_run_writes_task_results_row` | command="echo ok"; task_id="uuid-run-1"; columns_required="exit_code,stdout_tail,stderr_tail,duration_ms,finished_at" | happy_path | Q1 [AC-2.4] |
| 5 | `test_timeout_kills_child_no_orphan` | command="sleep 60"; timeout_seconds="1.0"; timeout_budget="1.0"; expects_orphan="false" | fault_injection | Q5 (NP-15) [AC-2.5] |
| 6 | `test_get_runs_returns_history_newest_first` | task_id="uuid-history-1"; n_runs="3"; order="newest_first" | happy_path | Q1 [AC-2.6] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR02-shell-token-banned | `forbidden_token == "shell=True"` | 2 |
| FR02-state-pending-then-terminal | `expected_terminal == "done"` | 3 |
| FR02-timeout-kills-no-orphan | `expects_orphan == "false"` | 5 |
| FR02-results-row-columns | `len(columns_required.split(",")) == 5` | 4 |
| FR02-history-newest-first | `order == "newest_first"` | 6 |
| FR02-happy-command-nonempty | `len(command) > 0` | 1, 4 |

**Properties** — OPTIONAL (Direction B). FR-02 has no clean algebraic invariant (subprocess execution is non-deterministic). Skip.

---

### FR-03: API key authentication — `X-API-Key` required on `/v1/*`; SHA-256 hashed storage, `hmac.compare_digest`; `revoked_at` invalidation; plaintext printed once at `key create`.

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-01 (401), NP-14 (hashed storage), NP-08 (T-01 spoofing)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_missing_api_key_returns_401` | endpoint="/v1/tasks"; api_key="" | failure | Q2 [AC-3.1] |
| 2 | `test_invalid_api_key_returns_401` | endpoint="/v1/tasks"; api_key="bogus-not-real-key-xyz"; invalid_token="bogus-not-real-key-xyz" | failure | Q2 [AC-3.2] |
| 3 | `test_api_keys_table_holds_no_plaintext` | row_key_hash="abc123...64-hex..."; row_id="1"; expected_hash_len="64" | happy_path | Q1 [AC-3.3] |
| 4 | `test_key_compare_uses_hmac_compare_digest` | compare_target="hmac.compare_digest"; expected_call="hmac.compare_digest" | security_control | Q6 (NP-08) [AC-3.4] |
| 5 | `test_revoked_key_treated_as_invalid` | key_id="k-revoked-1"; revoked_at_set="true" | failure | Q2 [AC-3.5] |
| 6 | `test_key_create_prints_plaintext_once_persists_hash` | scope="write"; expected_outputs="one_plaintext_one_hash"; printed_count="1" | happy_path | Q1 [AC-3.6] |
| 7 | `test_health_endpoints_no_auth_required` | endpoint="/healthz"; api_key="" | happy_path | Q1 [AC-3.7] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR03-missing-key-no-auth | `api_key == ""` | 1 |
| FR03-invalid-key-rejected | `invalid_token == "bogus-not-real-key-xyz"` | 2 |
| FR03-hash-len-64 | `expected_hash_len == "64"` | 3 |
| FR03-hmac-compare-digest | `expected_call == "hmac.compare_digest"` | 4 |
| FR03-revoked-rejected | `revoked_at_set == "true"` | 5 |
| FR03-print-once | `printed_count == "1"` | 6 |
| FR03-healthz-no-auth | `endpoint == "/healthz"` | 7 |

**Properties** — OPTIONAL (Direction B). FR-03 has a possible round-trip: `len(hash(plaintext)) == 64`. But this is a constant identity (already asserted as case-3 sub-assertion) — not a universal invariant over free inputs. Skip.

---

### FR-04: Scope authorisation — `read < write < admin` hierarchical; 403 must not disclose resource existence; one shared FastAPI dependency for every `/v1` route.

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-02 (403), NP-08 (T-03 EoP)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_read_scope_post_tasks_returns_403` | endpoint="POST /v1/tasks"; scope="read"; required="write" | failure | Q2 [AC-4.1] |
| 2 | `test_write_scope_delete_returns_403_no_resource_leak` | endpoint="DELETE /v1/tasks/{id}"; scope="write"; required="admin"; resource_exists="either" | failure | Q2 (NP-08) [AC-4.2] |
| 3 | `test_admin_scope_delete_succeeds` | endpoint="DELETE /v1/tasks/{id}"; scope="admin"; expected_status="204" | happy_path | Q1 [AC-4.3] |
| 4 | `test_single_authz_dependency_used_by_every_v1_route` | routes_scanned="all_v1"; expected_dependency="require_scope"; unique_dep_count="1" | architecture_constraint | Q6 (NFR-06) [AC-4.4] |
| 5 | `test_scope_hierarchy_admin_satisfies_write` | key_scope="admin"; required_scope="write"; expected_pass="true" | happy_path | Q1 [AC-4.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR04-read-fails-write-required | `required == "write"` | 1 |
| FR04-write-fails-admin-required | `required == "admin"` | 2 |
| FR04-admin-succeeds-delete | `scope == "admin"` | 3 |
| FR04-single-dep-count-1 | `unique_dep_count == "1"` | 4 |
| FR04-hierarchy-admin-contains-write | `expected_pass == "true"` | 5 |

**Properties** — OPTIONAL (Direction B). FR-04 has a hierarchy-monotonicity invariant (`admin >= write`, `write >= read`) but it is a constant ordering, not an algebraic invariant over a free input space. Skip.

---

### FR-05: Rate limiting — per-token DB-backed token bucket (`TASKQ_RATE_BURST` / `TASKQ_RATE_PER_SEC`); 429 + `Retry-After`; row-level lock in a single transaction; health endpoints exempt.

**Classification**: STATE_MACHINE
**Active Patterns**: NP-03 (429), NP-13 (concurrency — row-level lock)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_burst_over_limit_returns_429_with_retry_after` | burst_count="21"; burst_capacity="20"; expected_status="429"; expected_header="Retry-After" | boundary | Q3 [AC-5.1] |
| 2 | `test_rate_limit_recovers_after_refill` | rate_per_sec="5.0"; burst="20"; refill_seconds="4" | happy_path | Q1 [AC-5.2] |
| 3 | `test_bucket_state_shared_across_workers` | worker_a_bucket_value="5"; worker_b_bucket_value="5"; shared_db_row="true" | integration | Q7 [AC-5.3] |
| 4 | `test_bucket_update_uses_row_level_lock` | concurrent_workers="8"; expected_lock_call="with_for_update"; no_over_admit="true" | fault_injection | Q5 (NP-13) [AC-5.4] |
| 5 | `test_health_endpoints_exempt_from_rate_limit` | endpoint="/healthz"; burst_count="100"; expected_status="200" | happy_path | Q1 [AC-5.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR05-burst-over-capacity | `burst_count == "21"` | 1 |
| FR05-status-429 | `expected_status == "429"` | 1 |
| FR05-retry-after-header-present | `expected_header == "Retry-After"` | 1 |
| FR05-shared-bucket-state | `shared_db_row == "true"` | 3 |
| FR05-row-level-lock-used | `expected_lock_call == "with_for_update"` | 4 |
| FR05-healthz-exempt | `endpoint == "/healthz"` | 5 |
| FR05-burst-capacity-value | `burst_capacity == "20"` | 1 |

**Properties** — OPTIONAL (Direction B). FR-05 has a token-bucket conservation invariant but it is a continuous-time simulation rather than a discrete algebraic invariant. Skip.

---

### FR-06: Persistence layer and transaction boundaries — repository-only `sqlalchemy`; one `Session` per request via context manager; explicit eager loading (no N+1); `pool_pre_ping = True`.

**Classification**: INFRASTRUCTURE
**Active Patterns**: NP-06 (N+1 SQL count constant — perf cross-cut)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_no_sqlalchemy_imports_in_service_or_api` | layer="service"; layer2="api"; forbidden_import="sqlalchemy" | architecture_constraint | Q6 (NFR-06) [AC-6.1] |
| 2 | `test_one_session_per_request_context_manager` | request_id="req-1"; expected_sessions="1"; uses_context_manager="true" | happy_path | Q1 [AC-6.2] |
| 3 | `test_no_string_concatenated_sql_in_src` | src_root="03-development/src"; forbidden_patterns="f-string-sql,%-sql,+-sql" | security_control | Q6 (NP-08) [AC-6.3] |
| 4 | `test_list_endpoint_sql_count_is_constant` | row_count_small="1"; row_count_large="1000"; sql_count_small="3"; sql_count_large="3" | nfr_pattern | Q6 (NP-06) [AC-6.4] |
| 5 | `test_engine_pool_config_matches_env` | env_pool_size="5"; env_pre_ping="True"; expected_pool_size="5"; expected_pre_ping="True" | happy_path | Q1 [AC-6.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR06-no-sqlalchemy-outside-repo | `forbidden_import == "sqlalchemy"` | 1 |
| FR06-one-session-per-request | `expected_sessions == "1"` | 2 |
| FR06-uses-context-manager | `uses_context_manager == "true"` | 2 |
| FR06-no-string-sql | `len(forbidden_patterns.split(",")) == 3` | 3 |
| FR06-sql-count-constant | `sql_count_small == sql_count_large` | 4 |
| FR06-pool-size-matches-env | `env_pool_size == expected_pool_size` | 5 |
| FR06-pool-pre-ping-true | `env_pre_ping == expected_pre_ping` | 5 |

**Properties** — OPTIONAL (Direction B). FR-06 has an N+1-guard invariant (`sql_count(N) == sql_count(M) for all N, M`), already declared as case-4 sub-assertion. Skip a separate Properties table — the invariant is not a free-variable property test; it is a measurement.

---

### FR-07: Schema migration — Alembic v1→v2→v3; v3 moves `tasks.result_json` into `task_results` with real data migration; every revision reversible; no `op.execute("DROP TABLE ...")` shortcut.

**Classification**: DATA_ENTITY
**Active Patterns**: NP-10 (data round-trip — byte-identical)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_upgrade_downgrade_base_clean` | head_revision="v3"; base_revision="base"; expected_exit="0" | happy_path | Q1 [AC-7.1] |
| 2 | `test_v3_data_migration_round_trip_byte_identical` | sample_row_count="10"; sample_columns="exit_code,stdout_tail,stderr_tail,duration_ms,finished_at"; compare_mode="byte_identical" | round_trip | Q4 [AC-7.2] |
| 3 | `test_no_destructive_drop_table_shortcuts` | src_glob="migrations/versions/*.py"; forbidden_pattern="op.execute(\"DROP TABLE"; expected_hits="0" | security_control | Q6 (NP-08) [AC-7.3] |
| 4 | `test_every_revision_downgrade_works` | revisions="v1,v2,v3"; expected_each_downgrade_exit="0" | happy_path | Q1 [AC-7.4] |
| 5 | `test_v2_unique_index_survives_round_trip` | index_name="ix_tasks_name_unique"; before_round_trip="exists"; after_round_trip="exists" | happy_path | Q1 [AC-7.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR07-upgrade-exit-zero | `expected_exit == "0"` | 1 |
| FR07-round-trip-byte-identical | `compare_mode == "byte_identical"` | 2 |
| FR07-no-destructive-drop | `expected_hits == "0"` | 3 |
| FR07-three-revisions | `len(revisions.split(",")) == 3` | 4 |
| FR07-v2-index-survives | `before_round_trip == after_round_trip` | 5 |

**Properties** — OPTIONAL (Direction B). FR-07 has the cleanest algebraic invariant in the project: a true round-trip. Declare.

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR07-round-trip-byte-identical | `result_after_downgrade == sample_before` | 2 |

> The invariant `result_after_downgrade == sample_before` is a Python
> expression comparing two list/dict structures column-by-column after the
> full upgrade→sample-write→downgrade→upgrade cycle (SPEC.md §8 #12).
> It degrades to `needs_review` until a hypothesis `@given` property test
> exercises it (P4); self-consistency holds at P2 against the declared case.

---

### FR-08: Async runner — `asyncio.TaskGroup`; cap `TASKQ_MAX_CONCURRENT`; graceful drain to `TASKQ_DRAIN_TIMEOUT` (excess `interrupted`); timeout `kill()` + `await wait()`; `CancelledError` propagates.

**Classification**: STATE_MACHINE
**Active Patterns**: NP-13 (concurrency cap), NP-15 (timeout kill)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_runner_uses_task_group` | expected_manager="asyncio.TaskGroup"; found_manager="asyncio.TaskGroup" | happy_path | Q1 [AC-8.1] |
| 2 | `test_concurrency_capped_at_max_concurrent` | max_concurrent="8"; submit_count="20"; expected_inflight_at_peak="8" | nfr_pattern | Q6 (NP-13) [AC-8.2] |
| 3 | `test_timeout_kills_child_no_orphan` | command="sleep 60"; timeout_seconds="1.0"; expects_orphan="false" | fault_injection | Q5 (NP-15) [AC-8.3] |
| 4 | `test_shutdown_drains_inflight_within_budget` | drain_timeout="30.0"; in_flight_count="4"; drain_within_budget="true" | happy_path | Q1 [AC-8.4] |
| 5 | `test_cancelled_error_propagates_not_swallowed` | cancellation_signal="asyncio.CancelledError"; expected_propagates="true"; not_swallowed_by="except Exception" | failure | Q2 [AC-8.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR08-taskgroup-manager | `expected_manager == "asyncio.TaskGroup"` | 1 |
| FR08-concurrency-cap-respected | `expected_inflight_at_peak == "8"` | 2 |
| FR08-timeout-kills-no-orphan | `expects_orphan == "false"` | 3 |
| FR08-drain-within-budget | `drain_within_budget == "true"` | 4 |
| FR08-cancelled-propagates | `expected_propagates == "true"` | 5 |
| FR08-not-swallowed-by-except-exception | `not_swallowed_by == "except Exception"` | 5 |

**Properties** — OPTIONAL (Direction B). FR-08 has no algebraic invariant — async execution is non-deterministic by nature. Skip.

---

### FR-09: Health and observability — `/healthz` 200 liveness; `/readyz` 503 when DB unreachable **or** `alembic current` != head (fail closed); `/v1/metrics` (admin) counts + latency percentiles + rate-limit rejects.

**Classification**: API_ENDPOINT
**Active Patterns**: NP-07 (dependency fault — DB-down)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_healthz_returns_200_when_alive` | endpoint="/healthz"; expected_status="200" | happy_path | Q1 [AC-9.1] |
| 2 | `test_readyz_returns_503_when_db_down` | db_state="down"; expected_status="503"; detail_mentions_db="true" | fault_injection | Q5 (NP-07) [AC-9.2] |
| 3 | `test_readyz_returns_503_when_migration_behind_head` | alembic_current="v2"; head="v3"; expected_status="503"; detail_mentions_migration="true" | fault_injection | Q5 [AC-9.3] |
| 4 | `test_readyz_returns_200_when_healthy` | db_state="up"; migration_at_head="true"; expected_status="200" | happy_path | Q1 [AC-9.4] |
| 5 | `test_metrics_returns_required_series` | required_series="task_counts,latency_percentiles,rate_limit_rejects"; series_count="3" | happy_path | Q1 [AC-9.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR09-healthz-200 | `expected_status == "200"` | 1 |
| FR09-readyz-503-db-down | `expected_status == "503"` | 2 |
| FR09-detail-mentions-db | `detail_mentions_db == "true"` | 2 |
| FR09-readyz-503-migration-behind | `expected_status == "503"` | 3 |
| FR09-detail-mentions-migration | `detail_mentions_migration == "true"` | 3 |
| FR09-readyz-200-healthy | `expected_status == "200"` | 4 |
| FR09-metrics-three-series | `len(required_series.split(",")) == 3` | 5 |

**Properties** — OPTIONAL (Direction B). FR-09 has no algebraic invariant (readiness is a discrete predicate, not algebraic). Skip.

---

### FR-10: Error contract (RFC 7807) — all non-2xx `application/problem+json` with `type`/`title`/`status`/`detail`/`instance`/`correlation_id`; no internals in `detail`; `X-Correlation-Id` header linked to logs; full code map.

**Classification**: INTEGRATION
**Active Patterns**: NP-08 (T-07 info-disclosure)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_non_2xx_content_type_is_problem_json` | response_status="422"; expected_content_type="application/problem+json" | happy_path | Q1 [AC-10.1] |
| 2 | `test_problem_json_fields` | required_fields="type,title,status,detail,instance,correlation_id"; field_count="6" | happy_path | Q1 [AC-10.2] |
| 3 | `test_500_detail_no_internals` | trigger_status="500"; forbidden_in_detail="Traceback,SQLAlchemy,/src/,SELECT"; expected_clean="true" | security_control | Q6 (NP-08) [AC-10.3] |
| 4 | `test_correlation_id_in_header_and_logs` | response_header="X-Correlation-Id"; log_emitted="true"; same_id="true" | happy_path | Q1 [AC-10.4] |
| 5 | `test_each_error_code_exercised` | codes="401,403,404,409,422,429,503,500"; code_count="8" | integration | Q7 [AC-10.5] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR10-content-type-problem-json | `expected_content_type == "application/problem+json"` | 1 |
| FR10-six-fields-present | `len(required_fields.split(",")) == 6` | 2 |
| FR10-detail-no-internals | `expected_clean == "true"` | 3 |
| FR10-correlation-id-header-and-log | `same_id == "true"` | 4 |
| FR10-eight-codes-covered | `len(codes.split(",")) == 8` | 5 |

**Properties** — OPTIONAL (Direction B). FR-10 has no algebraic invariant (RFC 7807 is a structural contract, not algebraic). Skip.

---

## Non-Functional Requirement Test Cases (Integration-Level Verifier)

> NFR Layering & Parameterization: only NFRs whose canonical verifier runs at
> **integration layer** (real ASGI app, real DB, real subprocess, real
> benchmark) get concrete `Inputs` + `Sub-assertions` here. Unit/Static-layer
> NFRs are isolated in the "Deferred to Downstream Phases" table below — those
> NFRs are verified by `harness_cli.py` AST/lint gates and by the project's
> own unit tests, not by a TEST_SPEC.md case row.

### NFR-01: Performance — `GET /v1/tasks/{id}` p95 < 30ms and `GET /v1/tasks?limit=50` p95 < 80ms at 10k rows; list SQL statement count constant.

**Classification**: PERFORMANCE
**Active Patterns**: NP-06 (latency SLA)
**Layer**: performance (integration-level benchmark)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_get_by_id_p95_under_30ms_at_10k` | row_count="10000"; endpoint="GET /v1/tasks/{id}"; budget_ms="30"; iterations="200" | nfr_pattern | Q6 (NP-06) [AC-N1.1] |
| 2 | `test_list_p95_under_80ms_at_10k` | row_count="10000"; endpoint="GET /v1/tasks?limit=50"; budget_ms="80"; iterations="200" | nfr_pattern | Q6 (NP-06) [AC-N1.2] |
| 3 | `test_list_endpoint_sql_count_is_constant` | row_count_small="1"; row_count_large="1000"; sql_count_small="3"; sql_count_large="3" | nfr_pattern | Q6 (NP-06) [AC-N1.3] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR01-get-by-id-budget | `budget_ms == "30"` | 1 |
| NFR01-get-by-id-iterations | `iterations == "200"` | 1 |
| NFR01-list-budget | `budget_ms == "80"` | 2 |
| NFR01-list-iterations | `iterations == "200"` | 2 |
| NFR01-list-10k-row-count | `row_count == "10000"` | 1, 2 |
| NFR01-sql-count-constant | `sql_count_small == sql_count_large` | 3 |

**Properties** — Skip. NFR-01 is measurement-based; algebraic invariants do not apply.

---

### NFR-12: System verification target — `make verify-system` chains `alembic upgrade head` → full suite → health smoke → migration round-trip; exit 0 and `verify-system: PASS` on stdout.

**Classification**: INFRASTRUCTURE
**Active Patterns**: NP-10 (migration round-trip), NP-07 (DB-down → 503)
**Layer**: integration (end-to-end Make target)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_make_verify_system_exits_zero_and_prints_pass` | make_target="verify-system"; expected_exit_code="0"; expected_stdout_contains="verify-system: PASS"; chain_steps="upgrade_head,pytest,health_smoke,downgrade_base,upgrade_head" | integration | Q7 [AC-N12.1] |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR12-exit-zero | `expected_exit_code == "0"` | 1 |
| NFR12-stdout-pass-marker | `expected_stdout_contains == "verify-system: PASS"` | 1 |
| NFR12-chain-five-steps | `len(chain_steps.split(",")) == 5` | 1 |

**Properties** — Skip. NFR-12 is a sequencing/observation contract; algebraic invariants do not apply.

---

## Deferred to Downstream Phases (Unit/Static NFRs)

> The following NFRs are verified at **unit** or **static** layer — the
> canonical verifier is a Python AST/lint gate (`harness_cli.py` scan) or a
> project's own `tests/unit/` case, not a TEST_SPEC.md Inputs/Sub-assertions
> row. Per NFR Layering Hard Rule, they are listed here for completeness;
> their named test functions live in `TEST_INVENTORY.yaml` and must be
> implemented by the P3 test author at the declared layer.

| # | NFR | Test Function | Layer | Title |
|---|---|---|---|---|
| 1 | NFR-02 | `test_no_shell_eval_exec_in_src` | unit | shell=True/eval(/exec( absent in `03-development/src/` (AC-N2.1) |
| 2 | NFR-02 | `test_no_string_concatenated_sql_in_src` | unit | f-string/%/+ built SQL absent (AC-N2.2) |
| 3 | NFR-02 | `test_api_keys_table_holds_no_plaintext` | integration | api_keys table holds only SHA-256 hex (AC-N2.3; also declared in FR-03) |
| 4 | NFR-02 | `test_bandit_zero_high_zero_medium` | unit | bandit reports 0 HIGH / 0 MEDIUM (AC-N2.4) |
| 5 | NFR-03 | `test_no_bare_except_in_src` | unit | no bare `except:` / `except Exception: pass` (AC-N3.1) |
| 6 | NFR-03 | `test_cancelled_error_propagates_not_swallowed` | unit | CancelledError not swallowed by `except Exception` (AC-N3.2; also declared in FR-08) |
| 7 | NFR-03 | `test_readyz_returns_503_when_db_down` | integration | DB-down → /readyz 503 (AC-N3.3; also declared in FR-09) |
| 8 | NFR-03 | `test_migration_failure_rolls_back` | integration | failed migration → DB at prior revision (AC-N3.4) |
| 9 | NFR-04 | `test_redaction_replaces_matching_lines` | unit | secret regex → `[REDACTED]` whole-line replacement (AC-N4.1) |
| 10 | NFR-04 | `test_db_url_password_never_logged_or_emitted` | unit | DB URL password absent from log / error / metrics (AC-N4.2) |
| 11 | NFR-04 | `test_key_create_prints_plaintext_once_persists_hash` | unit | key plaintext printed once (AC-N4.3; also declared in FR-03) |
| 12 | NFR-05 | `test_public_symbols_have_fr_or_nfr_docstring` | unit | public docstrings carry `[FR-XX]`/`[NFR-XX]` at 100% (AC-N5.1) |
| 13 | NFR-05 | `test_every_endpoint_has_summary_and_description` | integration | /openapi.json endpoint summary+description (AC-N5.2) |
| 14 | NFR-06 | `test_lint_imports_exit_zero` | unit | `lint-imports` exits 0 against `.importlinter` (AC-N6.1) |
| 15 | NFR-06 | `test_sqlalchemy_forbidden_outside_repository` | unit | sqlalchemy import blocked from service/api (AC-N6.2) |
| 16 | NFR-06 | `test_importlinter_file_present_with_both_contracts` | unit | `.importlinter` has layers + forbidden contracts (AC-N6.3) |
| 17 | NFR-07 | `test_requirements_txt_pinned_with_double_equals` | unit | requirements.txt uses `==` pinning (AC-N7.1) |
| 18 | NFR-07 | `test_requirements_lock_locks_transitives` | unit | requirements.lock covers transitive deps (AC-N7.2) |
| 19 | NFR-07 | `test_every_dep_license_in_allowlist` | unit | pip-licenses license in allowlist (AC-N7.3) |
| 20 | NFR-07 | `test_sbom_has_required_fields_per_dep` | unit | SBOM.json has name/version/license/direct-transitive (AC-N7.4) |
| 21 | NFR-08 | `test_mutation_testing_feature_enabled` | unit | harness_config.json sets `features.mutation_testing: true` (AC-N8.1) |
| 22 | NFR-08 | `test_mutation_score_at_least_70` | unit | mutmut score ≥ 70 over service/ + repository/ (AC-N8.2) |
| 23 | NFR-09 | `test_pytest_skipped_count_zero` | unit | `pytest -q` skipped count = 0 (AC-N9.1) |
| 24 | NFR-09 | `test_zero_assertion_free_tests` | unit | every test has ≥1 assert (AC-N9.2) |
| 25 | NFR-09 | `test_v3_data_migration_round_trip_byte_identical` | integration | v3 data migration round-trip byte-identical (AC-N9.3; also declared in FR-07) |
| 26 | NFR-09 | `test_no_test_exclusion_paths` | unit | no `--ignore`/`-k`/`--deselect`/`collect_ignore` (AC-N9.4) |
| 27 | NFR-10 | `test_integration_coverage_at_least_80_percent` | unit | integration coverage ≥ 80% (AC-N10.1) |
| 28 | NFR-10 | `test_integration_tests_use_asgi_transport_not_direct_handler` | unit | integration tests use `httpx.ASGITransport` (AC-N10.2) |
| 29 | NFR-10 | `test_each_error_code_exercised` | integration | every error code exercised (AC-N10.3; also declared in FR-10) |
| 30 | NFR-11 | `test_project_mi_at_least_80` | unit | project MI ≥ 80 (AC-N11.1) |
| 31 | NFR-11 | `test_no_function_cc_above_10` | unit | no function CC > 10 (AC-N11.2) |
| 32 | NFR-11 | `test_file_and_dir_size_limits` | unit | ≤400 lines/file; ≤15 files/dir (AC-N11.3) |
| 33 | NFR-11 | `test_api_handlers_within_40_lines` | unit | API handlers ≤40 lines (AC-N11.4) |

---

## Cross-Cutting Test Cases

### NFR Integration (system-wide NFR cross-cuts — declared above per-FR/NFR; this section enumerates the cross_cutting performance / security / reliability / redaction / documentation / layering / licensing / mutation / testability / integration / maintainability / verifiability groups from `TEST_INVENTORY.yaml` for traceability; each name appears in its primary FR/NFR section above).

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_get_by_id_p95_under_30ms_at_10k` | performance | cross-cut: NFR-01 (declared above) |
| 2 | `test_list_p95_under_80ms_at_10k` | performance | cross-cut: NFR-01 (declared above) |
| 3 | `test_list_endpoint_sql_count_is_constant` | performance | cross-cut: NFR-01/FR-06 (declared above) |
| 4 | `test_concurrency_capped_at_max_concurrent` | performance | cross-cut: FR-08 (declared above) |
| 5 | `test_no_shell_eval_exec_in_src` | security | cross-cut: NFR-02 (deferred) |
| 6 | `test_no_string_concatenated_sql_in_src` | security | cross-cut: NFR-02 / FR-06 (deferred + declared above) |
| 7 | `test_api_keys_table_holds_no_plaintext` | security | cross-cut: FR-03 / NFR-02 (declared above) |
| 8 | `test_bandit_zero_high_zero_medium` | security | cross-cut: NFR-02 (deferred) |
| 9 | `test_no_bare_except_in_src` | reliability | cross-cut: NFR-03 (deferred) |
| 10 | `test_cancelled_error_propagates_not_swallowed` | reliability | cross-cut: FR-08 / NFR-03 (declared above + deferred) |
| 11 | `test_readyz_returns_503_when_db_down` | reliability | cross-cut: FR-09 / NFR-03 (declared above + deferred) |
| 12 | `test_migration_failure_rolls_back` | reliability | cross-cut: NFR-03 (deferred) |
| 13 | `test_redaction_replaces_matching_lines` | redaction | cross-cut: NFR-04 (deferred) |
| 14 | `test_db_url_password_never_logged_or_emitted` | redaction | cross-cut: NFR-04 (deferred) |
| 15 | `test_key_create_prints_plaintext_once_persists_hash` | redaction | cross-cut: FR-03 / NFR-04 (declared above + deferred) |
| 16 | `test_public_symbols_have_fr_or_nfr_docstring` | documentation | cross-cut: NFR-05 (deferred) |
| 17 | `test_every_endpoint_has_summary_and_description` | documentation | cross-cut: NFR-05 (deferred) |
| 18 | `test_lint_imports_exit_zero` | layering | cross-cut: NFR-06 (deferred) |
| 19 | `test_sqlalchemy_forbidden_outside_repository` | layering | cross-cut: FR-06 / NFR-06 (declared above + deferred) |
| 20 | `test_importlinter_file_present_with_both_contracts` | layering | cross-cut: NFR-06 (deferred) |
| 21 | `test_requirements_txt_pinned_with_double_equals` | licensing | cross-cut: NFR-07 (deferred) |
| 22 | `test_requirements_lock_locks_transitives` | licensing | cross-cut: NFR-07 (deferred) |
| 23 | `test_every_dep_license_in_allowlist` | licensing | cross-cut: NFR-07 (deferred) |
| 24 | `test_sbom_has_required_fields_per_dep` | licensing | cross-cut: NFR-07 (deferred) |
| 25 | `test_mutation_testing_feature_enabled` | mutation | cross-cut: NFR-08 (deferred) |
| 26 | `test_mutation_score_at_least_70` | mutation | cross-cut: NFR-08 (deferred) |
| 27 | `test_pytest_skipped_count_zero` | testability | cross-cut: NFR-09 (deferred) |
| 28 | `test_zero_assertion_free_tests` | testability | cross-cut: NFR-09 (deferred) |
| 29 | `test_v3_data_migration_round_trip_byte_identical` | testability | cross-cut: FR-07 / NFR-09 (declared above + deferred) |
| 30 | `test_no_test_exclusion_paths` | testability | cross-cut: NFR-09 (deferred) |
| 31 | `test_integration_coverage_at_least_80_percent` | integration | cross-cut: NFR-10 (deferred) |
| 32 | `test_integration_tests_use_asgi_transport_not_direct_handler` | integration | cross-cut: NFR-10 (deferred) |
| 33 | `test_each_error_code_exercised` | integration | cross-cut: FR-10 / NFR-10 (declared above + deferred) |
| 34 | `test_project_mi_at_least_80` | maintainability | cross-cut: NFR-11 (deferred) |
| 35 | `test_no_function_cc_above_10` | maintainability | cross-cut: NFR-11 (deferred) |
| 36 | `test_file_and_dir_size_limits` | maintainability | cross-cut: NFR-11 (deferred) |
| 37 | `test_api_handlers_within_40_lines` | maintainability | cross-cut: NFR-11 (deferred) |
| 38 | `test_make_verify_system_exits_zero_and_prints_pass` | verifiability | cross-cut: NFR-12 (declared above) |

### Deployment Smoke

> Deployment-smoke verification is integrated into NFR-12
> (`test_make_verify_system_exits_zero_and_prints_pass`); no separate
> section is needed.

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 10 |
| NFRs covered (per-section, integration-level) | 2 (NFR-01, NFR-12) |
| NFRs deferred (unit/static) | 10 (NFR-02, NFR-03, NFR-04, NFR-05, NFR-06, NFR-07, NFR-08, NFR-09, NFR-10, NFR-11) |
| Total test cases (declared + deferred) | 92 (matches `TEST_INVENTORY.yaml` 92 entries) |
| By type: happy_path | 18 |
| By type: validation/failure | 12 |
| By type: boundary | 2 |
| By type: state_transition | 1 |
| By type: round_trip | 1 |
| By type: fault_injection | 4 |
| By type: nfr_pattern | 8 |
| By type: integration | 6 |
| By type: security_control | 4 |
| By type: architecture_constraint | 2 |
| By type: performance | 3 |
| Active NFR patterns applied | NP-01, NP-02, NP-03, NP-04, NP-06, NP-07, NP-08, NP-10, NP-12, NP-13, NP-14, NP-15 |
| FR Properties tables | 1 (FR-07 round-trip) |

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.13.0 | TEST_SPEC.md shape per check-test-spec-consistency v2.13.0*
